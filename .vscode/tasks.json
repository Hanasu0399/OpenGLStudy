{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake 构建",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}\\MyOpenGLProject\\build",
                "--config",
                "Debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "使用 CMake 编译 OpenGL 项目 (Lesson1.cpp)"
        }
    ]
}